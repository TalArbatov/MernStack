import React from 'react';

const CreateUser = (props) => {
    return (
        <div>
            <table>
                <thead>
                    
                </thead>
            </table>
        </div>
    );
};

export default CreateUser;